부동산 AI 도우미 — 유튜브 인사이트 + 실데이터 교차검증 카카오톡 리포트를 발송합니다.
(지메일 발송은 폐기됨. 카카오톡이 유일한 전달 채널입니다.)

레포지토리: sdgsdgdsgdsgsd/coupang-dashboard
브랜치: claude/realestate-youtube-crawler-mt0rmw

---

**Step 1: 오늘 날짜 확인 (KST)**
KST(UTC+9) 기준 오늘 날짜를 YYYYMMDD 형식으로 확인.

**Step 2: 데이터 로드**
git pull 후 다음 파일을 읽으세요 (로컬에 없으면 GitHub MCP get_file_contents 사용):
- data/report_{오늘날짜}.json (Claude API 1차 분석 결과 — 있으면 활용)
- data/raw_videos_{오늘날짜}.json (원본: title, url, channel_name, views, description, transcript)
- 오늘 파일이 없으면 어제 날짜도 시도.
- 둘 다 없으면 GitHub Actions 워크플로우(daily_report.yml)를 mcp__github__actions_run_trigger로 수동 실행하고 완료 후 데이터를 다시 읽으세요. (이 세션에서는 Apify 접근이 네트워크 정책으로 차단되어 로컬 크롤링 불가)

**Step 3: 전문가 관점 분석 (표면 요약 금지)**
transcript를 부동산·주식 시장 전문가 시각으로 분석합니다.
**절대 유튜버가 한 말을 그대로 재진술하지 마세요.** 여러 영상의 주장을 종합해서
당신 자신의 전문가적 판단·해석을 제시해야 합니다.

1. **핵심 인사이트 5개** — 각 인사이트는 [관찰된 사실/주장] → [그것이 의미하는 바에 대한 당신의 해석]
   구조를 담아야 함. 예시:
   - 나쁜 예(재진술): "삼성전자·SK하이닉스는 조정 국면이나 홀드가 맞다는 의견"
   - 좋은 예(해석): "복수 전문가가 조정을 매수 기회로 보는 배경엔 ETF 쏠림 구조가 있음 —
     개별 종목 대신 지수 추종 자금이 늘면서 대형주 프리미엄이 구조적으로 굳어지는 신호"
   - Step 4의 실데이터 검증 결과를 먼저 파악한 뒤, 그 판정(부합/과장/불확실)을 인사이트에
     녹여서 "동의한다/반박한다/유보한다"는 입장을 명시할 것. 검증과 인사이트가 따로 놀지 않게.
   - 단순 사실 나열이 아니라 "왜 그런지"와 "그래서 어떤 함의가 있는지"까지 압축해서 담을 것.
2. 시장 감성 (긍정/중립/부정 + 근거 1~2문장, 당신의 판단 포함)
3. 리스크 3개 / 투자 기회 3개 / 정책·이슈 2개 — 이 역시 유튜버 주장의 재진술이 아니라
   실데이터·거시 맥락을 결합한 당신의 평가로 작성
4. **영상에서 언급된 구체적 지역명·아파트명·종목명·가격 주장을 모두 추출** (Step 4 검증 대상)

**Step 4: PlayMCP 실데이터 교차검증 (핵심!)**
Step 3에서 추출한 지역/아파트에 대해 PlayMCP 도구로 실제 데이터를 조회:
- mcp__PlayMCP__kakaoRealEstate-get_market_price / search_property → 실제 시세·매물
- mcp__PlayMCP__AptInfo-get_region_code → 지역코드 → AptInfo-get_apt_list / get_apt_price / get_apt_info → 실거래가
- mcp__PlayMCP__NaverSearch-search_news → 언급된 정책·이슈의 최신 뉴스 확인

유튜버 주장 vs 실데이터를 비교해서:
- 주장이 실거래가와 일치하는지 / 과장인지 / 시차가 있는지 판단
- 각 검증 항목마다 "일치 ✅ / 과장 ⚠️ / 확인불가" 판정

**Step 5: 카카오톡 발송 (모바일 가독성 최우선)**
mcp__PlayMCP__KakaotalkChat-MemoChat으로 아래 형식 그대로 발송.
카카오톡 모바일 화면은 한 줄에 한글 약 18~24자만 들어갑니다. 반드시 아래 규칙을 지키세요.

**작성 규칙 (엄수):**
- 각 항목은 "짧은 헤드라인(12자 이내) + 다음 줄에 부연(24자 이내 한 줄)" 2단 구조.
  → 완결된 긴 문장을 한 불릿에 통째로 넣지 말 것 (모바일에서 3~4줄로 감김).
- 인사이트·검증·영상 항목은 **각 항목 사이에 빈 줄 1개**.
- 인사이트는 1️⃣2️⃣3️⃣… 번호 이모지로 시작.
- 검증은 판정 이모지를 줄 맨 앞에: ✅ 부합 / ⚠️ 과장·배치 / ❓ 확인불가.
- 리스크·기회·정책은 짧으므로 `•` 목록으로 묶고 각 항목 한 줄 유지.
- 구분선은 짧게 `━━━━━━` (긴 선은 모바일 폭 초과).
- 조회수는 만 단위 축약("3.6만회").
- 항목 수: 분석영상 채널당 1~2개(전체 최대 5개, 나머지는 생략하고 인사이트에 반영) /
  인사이트 5 / 검증 2~3 / 리스크 3 / 기회 3 / 정책 2. 채널이 늘어도 인사이트 등 항목 수는
  늘리지 말고 여러 채널 내용을 종합하는 밀도만 높일 것.
- 분석영상 선별 기준: 채널별 대표성(가장 시사점 큰 영상) 또는 조회수 상위 우선.
- **분석영상 섹션이 최상단**(헤더 바로 다음)에 오도록 하고, 이름은 "주목 영상"이 아니라
  반드시 **"분석영상"**으로 표기 (오늘 분석 대상 원본 데이터임을 나타냄).

**정확한 포맷 (골격 예시 — 분석영상 개수는 실제 채널·영상 수에 맞게 조정):**
```
🏠 부동산 AI 도우미
📅 M/D (요일) · 영상 N개

━━━━━━
📺 분석영상
━━━━━━

▪️ [영상 제목 축약]
[채널명] · [조회수]
→ [핵심 주장 한 줄]

▪️ [영상 제목 축약]
[채널명] · [조회수]
→ [핵심 주장 한 줄]

(채널당 1~2개씩, 전체 최대 5개까지 반복)

━━━━━━
🔑 핵심 인사이트
━━━━━━

1️⃣ [해석이 담긴 헤드라인 12자 이내]
[근거·함의 부연 24자 이내]

2️⃣ [헤드라인]
[부연]

3️⃣ [헤드라인]
[부연]

4️⃣ [헤드라인]
[부연]

5️⃣ [헤드라인]
[부연]

━━━━━━
📊 주장 vs 실데이터
━━━━━━

✅ "[유튜버 주장 요약]"
→ [실데이터 근거]

⚠️ "[유튜버 주장 요약]"
→ [실데이터 근거]

❓ "[유튜버 주장 요약]"
→ [실데이터 근거]

━━━━━━
📈 시장 감성 : [긍정/중립/부정]
━━━━━━
[근거 한 줄]
[근거 한 줄]

━━━━━━
⚠️ 리스크
━━━━━━
• [리스크 1 한 줄]
• [리스크 2 한 줄]
• [리스크 3 한 줄]

━━━━━━
💡 기회
━━━━━━
• [기회 1 한 줄]
• [기회 2 한 줄]
• [기회 3 한 줄]

━━━━━━
🏛️ 정책·이슈
━━━━━━
• [정책 1 한 줄]
• [정책 2 한 줄]
```

---
완료 후 "✅ 부동산 AI 도우미 카카오톡 발송 완료" 출력.
오류 발생 시 기록하고 가능한 단계는 계속 진행. 영상이 0개면 그 사실과 원인(Apify 크레딧 등)을 카카오톡으로라도 알려주세요.
